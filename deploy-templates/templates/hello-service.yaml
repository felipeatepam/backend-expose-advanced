apiVersion: v1
kind: Service
metadata:
  name: hello-service
  namespace: 0oqfemca-expose-advanced-dev
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app.kubernetes.io/instance: backend-expose-advanced
    app.kubernetes.io/name: backend-expose-advanced
  type: ClusterIP
